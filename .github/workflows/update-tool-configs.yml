# Some workflow
name: Update tool configs
on: 
    workflow_dispatch:
    # TO_DO: schedule
    
jobs:
  update-configs:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: actions/checkout@v3 # downloads current repo
      with: 
        path: '${{github.workspace}}/current'
    - uses: actions/checkout@v3 # downloads example repo
      with:
        repository: 'actions/reusable-workflows'
        path: '${{github.workspace}}/example'
    - run: |
        ls "${{github.workspace}}/current"
        echo "---------------------"
        ls "${{github.workspace}}/example"
